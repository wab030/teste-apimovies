<!DOCTYPE html>
<html lang="pt-br">

<head>
	<meta charset="utf-8" />
	<title>Formulário de Inclusão de Obra de Arte</title>
	<style>
		* {
			margin: 0;
			padding: 0;
			box-sizing: border-box;
		}

		body {
			font-family: Arial, Helvetica, sans-serif;
		}

		.container {
			background-image: linear-gradient(to right, red, yellow);
			border-radius: 5px;
			width: 50%;
			margin: 2rem auto;
			padding: 2rem 0;
			;
			box-shadow: 10px 10px 5px black;
		}

		.container h1 {
			text-align: center;
			color: rgb(55, 51, 51);
			margin: 1rem;
		}

		.form {
			margin: 30px;
			position: relative;
			/* background-color: white; */
		}

		.form input {
			width: 100%;
			display: block;
			font-size: 1.5rem;
			border: none;
			border-radius: 5px;
			margin: 5px auto;
			padding: 5px;
		}

		/* .form button::before {
			content: "";
			position: absolute;
			bottom: 0;
			left: 0;
			height: 40px;
			width: 3px;
			background-color: rgb(111, 107, 107);
			border-radius: 5px;
			transform: scaleY(0);
			z-index: 0;
			transition: transform .2s,
						width 0.9s .2s; 
		}

		.form button:hover::before {
			z-index: 1;
			transform: scaleY(1);
			width: 100%;
		} */

		.form button {
			width: 100%;
			height: 40px;
			border: none;
			border-radius: 10px;
			background-color: #000;
			color: white;
			font-size: 1.5rem;
			margin-top: 1rem;
			position: relative;
			z-index: 100;
			transition: all .2s;
		}

		.form button:hover {
			transform: translateY(+5px);
			background-color: rgb(111, 107, 107);
			/* filter: brightness(2); */
		}

		.errors {
			color: white;
			font-size: 1.5rem;
			text-align: center;
		}

		.errors li {
			list-style: none;
		}
	</style>
</head>

<body>
	<div class="container">
		<h1>Formulário para inclusão de obra de arte</h1>
		<section class="errors">
			<% if(errors) { %>
				<ul>
					<% for(let i=0; i < errors.length; i++){ %>
						<li>
							<%= errors[i].msg %>
						</li>
						<% } %>
				</ul>
				<% } %>
		</section>
		<form class="form" action="/obra/salvar?op=<%= op %>" method="post">
			<input type="text" name="nome" placeholder="Digite o nome da obra" value="<%= painting.nome %>">
			<input type="number" name="ano" placeholder="Digite o ano da obra" value="<%= painting.ano %>">
			<input type="text" name="artista" placeholder="Digite o nome da autora ou autor da obra" value="<%= painting.artista %>">
			<input type="text" name="urlimagem" placeholder="Digite a url da foto da imagem" value="<%= painting.urlimagem %>">
			<button type="submit" value="enviar">Enviar</button>
		</form>
	</div>

</body>

</html>